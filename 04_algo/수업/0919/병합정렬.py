def merge_sort(m):
    if len(m) == 1:
        return m
    left = []
    right = []
    middle = len(m) / 2
    for i in range(middle-1):
        left.append(i)

    for j in range(middle, len(m)):
        right.append(i)






